/.phpunit.cache
/bootstrap/ssr
/node_modules
# NOTE: public/build is intentionally COMMITTED. The Forge host (Ubuntu 18.04,
# glibc 2.27, Node 8) cannot run the Node 18+ build toolchain, so assets are
# built locally and shipped. After changing CSS/JS: `npm run build` then commit
# public/build. See memory: forge-build-node-version. Do NOT re-ignore this.
# /public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.nova
/.vscode
/.zed
.DS_Store

# SQLite database
/database/*.sqlite
/database/*.sqlite-journal

# Imported comic art lives in storage; keep the incoming folder, ignore its contents
/storage/app/comics/incoming/*
!/storage/app/comics/incoming/.gitkeep
/storage/app/public/comics/*
