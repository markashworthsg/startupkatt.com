; The Forge host (Ubuntu 18.04 / Node 8) cannot run the modern build toolchain.
; Skip dependency lifecycle scripts so `npm ci` / `npm install` don't crash on
; esbuild's postinstall. Assets are pre-built and committed under public/build;
; the server never builds. Explicit `npm run build` is unaffected (still works locally).
ignore-scripts=true
