<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        // Real comics come in via `php artisan comics:import`.
        // Add demo data here if you want something to look at locally, e.g.:
        //
        // \App\Models\Comic::factory()->count(5)->create();
    }
}
